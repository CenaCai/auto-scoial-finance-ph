# 依赖

# 新闻采集
feedparser>=6.0.0
requests>=2.31.0

# 图片生成
Pillow>=10.0.0

# Google Sheets
gspread>=6.0.0
oauth2client>=4.1.3
pyyaml>=6.0

# AdsPower API（通过 HTTP 调用）
# 无需额外依赖

# 浏览器自动化（备选方案）
playwright>=1.40.0

# 工具
python-dotenv>=1.0.0
